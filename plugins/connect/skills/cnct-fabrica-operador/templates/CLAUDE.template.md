# Memory — {{NOME}}

> **CAMADA 0 — Hot cache pessoal (só o DELTA).**
> A **espinha** do dois-cérebros — protocolo de início de sessão em camadas, regra de escrita/wikilinks, calibração "identificador nunca vem sozinho", check de atualizações, "antes de tocar código" — **não vive aqui**. É mecanismo: vem sempre de `config/protocolo-mecanismo.md` do Connect, injetada no contexto pelo hook de início de sessão — nunca copiada ou reescrita em arquivo de operador (ver `GLOSSARIO.md#espinha-e-mecanismo` no repo do plugin, se quiser o porquê). Este arquivo guarda só o que é **seu** e não deriva da espinha.

---

## Quem sou

{{NOME}} — {{PAPEIS}}.

> Papéis efetivos e paths por cliente vivem em `clientes/{slug}/config.md` — resolvidos via [[meu-config]]. Não copiar aqui (evita drift: identidade cross-cliente não carrega dado de um cliente específico).

## Memória profunda

`_cerebro/memory/` — índice em [[_cerebro/memory/MEMORY|_cerebro/memory/MEMORY.md]]

## People — resolução lazy (por cliente)

> Pessoas são **conhecimento do cliente** → fonte no coletivo daquele cliente. Resolver o nome quando alguém for mencionado; nunca manter roster pessoal.

## Vocabulário e estrutura de entrega

> Fonte única no **coletivo ativo** (processo da squad + metodologia). Não manter tabela de termos nem "onde vive cada coisa" aqui — a Camada 1 do coletivo já aponta (evita o drift de cópia local desatualizada).

## Suas convenções pessoais

> Espaço para convenções genuinamente suas, sem equivalente no coletivo (ao nascer, vazio).
