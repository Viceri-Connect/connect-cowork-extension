Copilot-specific instructions for `cnct-nucleo-conhecimento`.

Purpose: provide Copilot with local invocation recommendations for `iniciar_sessao`,
`estado_sessao`, and `resolver`. This is a local-only augmentation and does not
affect the Claude `SKILL.md`.
