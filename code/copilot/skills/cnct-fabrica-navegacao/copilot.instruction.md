Copilot-specific instructions for `cnct-fabrica-navegacao`.

Purpose: guidance for Copilot adapter on how to present prompts and when to call
the bridge `iniciar_sessao` and `resolver` endpoints for Copilot flows.

This file is intentionally minimal — `SKILL.md` in `plugins/connect/skills/` remains
the canonical documentation for Claude marketplace.
